//datatypes
let a=10; //number
let b=10.5; //number
let c='10'; //string
let d=true; //boolean
let e=null; //null
let a; //undefined
let f=undefined; //undefined
let g=NaN; //NaN
let h=Infinity; //Infinity
let i=new Number(10); //object
let j=new String('10'); //object
let k=new Boolean(true); //object
let l=new Date(); //object
let m=new RegExp('/^[a-zA-Z0-9]+$/'); //object
let n=new Array(); //object
let o=new Object(); //object
